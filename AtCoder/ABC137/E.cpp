#include<iostream>
#include<vector>
#include<algorithm>
using namespace std;
#define ll long long

int main(){
  ll N, M, P;
  cin >> N >> M >> P;
  vector<ll> A, B, C;
  for(int i=0; i<N; ++i){
    ll a, b, c;
    cin >> a >> b >> c;
    A.push_back(a);
    B.push_back(b);
    C.push_back(c);
  }

  


  return 0;
}
